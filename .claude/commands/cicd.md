# 배포 가이드

다음 요구사항을 참고하여 배포 프로세스를 진행하십시오

1. GitHub에 배포를 진행하며, main 브랜치에 병합된 커밋을 기준으로 합니다.
2. CI/CD를 통해 자동으로 빌드가 되도록 합니다.
3. github pages를 통해 배포가 이루어지도록 설정합니다.
4. 빌드가 성공적으로 완료되면, 자동으로 배포가 진행되도록 설정합니다.
